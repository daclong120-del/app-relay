services:
  caddy:
    image: caddy:2
    restart: unless-stopped
    profiles:
      - production
    env_file:
      - path: .env
        required: false
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      api:
        condition: service_healthy
    networks:
      - app-relay

  api:
    image: ${DOCKERHUB_USERNAME}/app-relay-api:${IMAGE_TAG:-latest}
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    init: true
    env_file:
      - path: .env.api
        required: false
      - path: .env
        required: false
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - api-artifacts:/data/artifacts
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/v1/health"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - app-relay

  worker:
    image: ${DOCKERHUB_USERNAME}/app-relay-worker:${IMAGE_TAG:-latest}
    build:
      context: ..
      dockerfile: apps/worker/Dockerfile
    restart: unless-stopped
    init: true
    env_file:
      - path: .env.worker
        required: false
      - path: .env
        required: false
    depends_on:
      api:
        condition: service_healthy
    shm_size: "2gb"
    ports:
      - "127.0.0.1:6080:6080"
    volumes:
      - worker-avd:/home/worker/.android
      - worker-work:/app/apps/worker/work
    networks:
      - app-relay

volumes:
  api-artifacts:
  worker-avd:
  worker-work:
  caddy-data:
  caddy-config:

networks:
  app-relay:

